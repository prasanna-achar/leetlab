# Backend Configuration
BACKEND_PORT=8080
FRONTEND_PORT=80

# Database Configuration (for LeetLab application)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=leetlab

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS Origins (comma-separated list of allowed origins)
# For production, set this to your domain(s)
CORS_ORIGINS=http://localhost,http://localhost:80

# Judge0 Configuration
# The JUDGE0_API is automatically set to http://judge0-server:2358 in docker-compose
# If deploying separately, set this to your Judge0 instance URL

